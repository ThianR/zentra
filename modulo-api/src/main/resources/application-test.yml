spring:
  application:
    name: zentra-api
  datasource:
    url: jdbc:h2:mem:zentradb;DB_CLOSE_DELAY=-1
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 9090
